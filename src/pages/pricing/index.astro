---
import Layout from "../../layouts/Layout.astro";
import fetchApi from "@/utils/strapi";
import ComponentCollectionMapper from "@/components/ComponentCollectionMapper.vue";

const metaObj = {
  title:
    "Pricing | Open Source Observability Platform for Logs, Metrics, Traces, and More – Your Ultimate Dashboard for Alerts and Insights",
  description: "OpenObserve Cloud pricing plans - get started for free and scale as you grow."
};

const PageData = await fetchApi({
  endpoint: "api/pricing",
  query: { pLevel: "4", status: "draft" },
});
---

<Layout metaObj={metaObj}>
  <ComponentCollectionMapper data={PageData?.data?.body} client:load />
</Layout>
